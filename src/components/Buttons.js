import React from 'react'

const Buttons = ({button1}) => {
  return (
    <div>
      <buttons 
      className= "p-1 cursor-pointer bg-gray-200 m-auto rounded-xl bg-opacity-50 px-3">{button1}</buttons>
    </div>
  )
}

export default Buttons
